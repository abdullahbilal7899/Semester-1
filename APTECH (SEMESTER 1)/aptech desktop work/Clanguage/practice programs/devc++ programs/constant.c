#include<stdio.h>
main()
{
      const int SIDE =20;
      int area;
      area= SIDE*SIDE;
      
      printf("the area of the square with side: %d is: %d sq.units",SIDE,area);
      getch();
      }
      
      
