#include <stdio.h>
#include <limits.h>
int main()
{
    printf("storage size for int is:%d \n",  sizeof(int));
    printf("storage size for char is: %d \n", sizeof(char));
    getch();
    return 0;
    
}
