#include <stdio.h>
#include <limits.h>
#include<conio.h>
int main()
{ int a=10;
    printf("storage size for int is:%d \n",  sizeof(a));
    printf("storage size for char is: %d \n", sizeof(char));
    getch();
    
    return 0;
    
}
